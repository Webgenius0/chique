import { cn } from '@/lib/utils';
import React from 'react';

const CommonSectionTitle = () => {
    return (
        <h3 className={cn("text-4xl font-semibold text-center text-primary-dark")}>
            
        </h3>
    );
};

export default CommonSectionTitle;